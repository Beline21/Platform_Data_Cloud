# Ignorer les fichiers de données volumineux
airflow/data/airflow/*.csv
airflow/data/airflow/*.json
airflow/data/airflow/*.zip

# Ignore le dossier de l'environnement virtuel
.venv

# Ignorer les fichiers terraform
terraform.tfvars
.terraform/
*.tfstate
*.tfstate.*
